#                   GIT COMMANDS

# to check the status of files in git
git status 

# to mention 
git remote add orgin https://github.com/user_name/repo_name.git  

# to add single or multiple files
git add file1 file2 

# to add all files
git add . 

# to note the changes made in proj
git commit -m "commit message"

# to change the comiit msg
git commit --amend -m "new message"

# to push files on github server
git push origin -u 

# to remove file or multiple file before pushing commit changes
git rm -r --cached <filename> 

# to remove the uploaded folder
moved to root folder
git rm <dirname>

